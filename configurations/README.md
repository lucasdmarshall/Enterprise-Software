# Configurations

Environment templates and configuration presets.

- `env/.env.example` – Example environment variables for apps/services.
- Add CI/tooling configs (linters, formatters) here when you choose your stack.