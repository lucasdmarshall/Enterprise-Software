# Hardware

Device integrations and drivers used by POS terminals.

Suggested layout:
- `printers/` – Receipt printers and drivers.
- `scanners/` – Barcode scanners.
- `cash_drawers/` – Cash drawer control.
- `customer_displays/` – Customer-facing displays.