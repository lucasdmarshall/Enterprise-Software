# Database

Database schemas, migrations, and seeders.

Suggested layout:
- `schemas/` – DDL/schema definitions.
- `migrations/` – Incremental migration files.
- `seeders/` – Seed data for local/dev environments.

Choose your RDBMS/ORM (Postgres/MySQL/SQLite, Prisma/TypeORM/EF/NHibernate).